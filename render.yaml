services:
  - type: web
    name: context-aware-bot
    env: python
    plan: free
    runtime: python-3.12.0
    buildCommand: pip install -r requirements.txt
    startCommand: sh -c 'exec uvicorn backend.app:app --host 0.0.0.0 --port "${PORT:-8000}"'
    autoDeploy: true
